packages:
  - apps/*
  - packages/*
  - runtimes/*

allowBuilds:
  esbuild: true
  sharp: true

minimumReleaseAgeExclude:
  - hono@4.13.7

# Drizzle's legacy ESM loader otherwise retains a vulnerable esbuild server.
overrides:
  "@esbuild-kit/core-utils>esbuild": "0.25.12"
