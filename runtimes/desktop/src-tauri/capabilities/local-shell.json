{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "local-shell",
  "description": "Minimal permissions for the trusted bundled startup shell.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:allow-emit"]
}
